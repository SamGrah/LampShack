{
  "name": "src",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "styles": "for f in src/styles/less/*.less; do lessc --math=always \"$f\" \"src/styles/css/$(basename \"${f%.less}\").css\"; done",
    "screenshot": "node scripts/screenshot.mjs",
    "shots": "npm run build && npm run screenshot"
  },
  "dependencies": {
    "@astrojs/netlify": "^6.5.3",
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.12.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "less": "^4.4.0",
    "playwright": "^1.60.0",
    "typescript": "^5.9.3"
  }
}
